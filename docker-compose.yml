version: '3.8'
services:
backend:
  build: ./backend
  container_name: webapp-backend 
  ports: 
    - "5000:5000"
  environment:
    - NODE_ENV=development
    - PORT=5000

frontend:
  build: ./frontend
  container_name: webapp-frontend
  ports:
    - "3000:3000"
  depends_on:
    - backend